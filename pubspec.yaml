name: diplom_app
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  provider: ^5.0.0
  easy_mask: ^1.1.5
  http: ^0.13.3
  firebase_core: ^1.1.0
  firebase_auth: ^1.1.3
  shared_preferences: ^2.0.5
  intl: ^0.17.0
  qr_flutter: ^4.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
